<main>
    <div><h3>注文履歴詳細</h3></div>
    
    <div>
        <strong>注文情報</strong>
        注文日<%= @order_detail.created_at %> </br>
        配送先
        <%= @order_detail.order.postal_code %> </br>
        <%= @order_detail.order.address %> </br>
        <%= @order_detail.order.name %> </br>
        支払方法
        <!--%= @order_detail.payment_method %>--> </br>
        ステータス
        <!--%= @order_detail.states %>--> </br>
    </div>
    
    <div>
        <strong>請求情報</strong>
        商品合計 <%= @order_detail.price %> </br>
        配送料<%= @order_detail.order.shipping_cost %></br>
        ご請求額<%= @order_detail.order.total_payment %>
    </div>
    
    <div>
        <strong>注文内容</strong>
        
        <table class="table table-hover table-inverse">
        <thead>
            <tr>
                <th>商品名</th>
                <th>単価（税込）</th>
                <th>数量</th>
                <th>小計</th>
            </tr>
        </thead>
        
        <tobody>
            <!--% @order_detail.each do |order_detail| %>-->
            <tr>
            <!--    <th>%= order_detail.item.name %></th>-->
            <!--    <th>%= (order_detail.price * 1.1).floor %></th>-->
            <!--    <th>%= order_detail.amount %></th>-->
            <!--    <th>%= (order_detail.item.price * 1.1).floor * order_detail.amount %></th>-->
            </tr>
            <!--% end %>-->
        </tobody>
        </table>
    </div>
</main>